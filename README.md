# svg-readme
Generate animated SVG banners for your GitHub profile README — built with React components rendered through Satori, with hand-injected CSS animations for effects Satori can't produce on its own (drifting grid backgrounds, morphing wave paths, staggered fade-ins).
